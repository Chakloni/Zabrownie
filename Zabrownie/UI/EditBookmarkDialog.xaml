<Window x:Class="Zabrownie.UI.EditBookmarkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Bookmark" Height="250" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Title:" 
                   VerticalAlignment="Center" Margin="0,0,8,12"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TitleTextBox" 
                 Padding="4" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="URL:" 
                   VerticalAlignment="Center" Margin="0,0,8,12"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="UrlTextBox" 
                 Padding="4" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Folder:" 
                   VerticalAlignment="Center" Margin="0,0,8,12"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="FolderComboBox" 
                  IsEditable="True" Padding="4" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="0,0,8,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>