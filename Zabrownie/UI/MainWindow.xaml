<!-- MainWindow.xaml -->
<Window x:Class="Zabrownie.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Zabrownie"
        Height="800"
        Width="1200"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize">

    <Window.InputBindings>
        <KeyBinding Key="T"
                    Modifiers="Control"
                    Command="{Binding NewTabCommand}"/>

        <KeyBinding Key="W"
                    Modifiers="Control"
                    Command="{Binding CloseTabCommand}"/>

        <KeyBinding Key="Tab"
                    Modifiers="Control"
                    Command="{Binding NextTabCommand}"/>

        <KeyBinding Key="Tab"
                    Modifiers="Control+Shift"
                    Command="{Binding PrevTabCommand}"/>

        <KeyBinding Key="L"
                    Modifiers="Control"
                    Command="{Binding FocusAddressBarCommand}"/>

        <KeyBinding Key="Add"
                    Modifiers="Control"
                    Command="{Binding ZoomInCommand}"/>

        <KeyBinding Key="Subtract"
                    Modifiers="Control"
                    Command="{Binding ZoomOutCommand}"/>

        <KeyBinding Key="D0"
                    Modifiers="Control"
                    Command="{Binding ZoomResetCommand}"/>
    </Window.InputBindings>

    <Window.Resources>
        <!-- Colores principales -->
        <SolidColorBrush x:Key="BgPrimary"
                         Color="#1A1A1D"/>
        <SolidColorBrush x:Key="BgSecondary"
                         Color="#25252A"/>
        <SolidColorBrush x:Key="BgTertiary"
                         Color="#2D2D35"/>
        <!-- <SolidColorBrush x:Key="AccentColor"
                         Color="#8B5CF6"/> -->
        <!-- <SolidColorBrush x:Key="AccentHover"
                         Color="#A78BFA"/> -->
        <SolidColorBrush x:Key="TextPrimary"
                         Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary"
                         Color="#A0A0A8"/>
        <SolidColorBrush x:Key="BorderColor"
                         Color="#3A3A45"/>

        <!-- Opera GX style ScrollBar -->
        <Style x:Key="OperaGXScrollBar"
               TargetType="ScrollBar">
            <Setter Property="Width"
                    Value="10"/>
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Root"
                              Background="Transparent">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True">
                                <!-- Invisible buttons (disable arrows) -->
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand"
                                                  Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand"
                                                  Opacity="0"/>
                                </Track.IncreaseRepeatButton>

                                <!-- The oval thumb -->
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="ThumbBorder"
                                                        Width="6"
                                                        Margin="2"
                                                        Background="{DynamicResource AccentColor}"
                                                        CornerRadius="10"
                                                        Opacity="0.7"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver"
                                                             Value="True">
                                                        <Setter TargetName="ThumbBorder"
                                                                Property="Opacity"
                                                                Value="0.9"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging"
                                                             Value="True">
                                                        <Setter TargetName="ThumbBorder"
                                                                Property="Opacity"
                                                                Value="1"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OperaGXScrollViewer"
               TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility"
                    Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility"
                    Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ScrollContentPresenter Grid.Column="0"/>

                            <ScrollBar Grid.Column="1"
                                       Orientation="Vertical"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Style="{StaticResource OperaGXScrollBar}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para tabs -->
        <Style x:Key="TabButtonStyle"
               TargetType="Button">
            <Setter Property="Background"
                    Value="{StaticResource BgSecondary}"/>
            <Setter Property="Foreground"
                    Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderBrush"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Padding"
                    Value="16,8"/>
            <Setter Property="Margin"
                    Value="4,0,0,0"/>
            <Setter Property="FontSize"
                    Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8,8,0,0"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Border"
                                        Property="Background"
                                        Value="{DynamicResource BgTertiary}"/>
                            </Trigger>

                            <DataTrigger Binding="{Binding IsActive}"
                                         Value="True">
                                <Setter Property="Foreground"
                                        Value="{StaticResource TextPrimary}"/>
                                <Setter TargetName="Border"
                                        Property="Background"
                                        Value="{StaticResource BgTertiary}"/>
                                <Setter TargetName="Border"
                                        Property="BorderBrush"
                                        Value="{DynamicResource AccentColor}"/>
                                <Setter TargetName="Border"
                                        Property="BorderThickness"
                                        Value="0,2,0,0"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para botones de navegación -->
        <Style x:Key="NavButtonStyle"
               TargetType="Button">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Foreground"
                    Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Width"
                    Value="40"/>
            <Setter Property="Height"
                    Value="40"/>
            <Setter Property="FontSize"
                    Value="16"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Border"
                                        Property="Background"
                                        Value="{StaticResource BgTertiary}"/>
                                <Setter Property="Foreground"
                                        Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="Border"
                                        Property="Background"
                                        Value="{StaticResource BorderColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter Property="Opacity"
                                        Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para botones de acción -->
        <Style x:Key="ActionButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background"
                    Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground"
                    Value="{StaticResource TextPrimary}"/>
            <Setter Property="Width"
                    Value="Auto"/>
            <Setter Property="Padding"
                    Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Border"
                                        Property="Background"
                                        Value="{DynamicResource AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="Border"
                                        Property="Opacity"
                                        Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Border exterior con sombra -->
    <Border Background="{StaticResource BgPrimary}"
            CornerRadius="12"
            BorderThickness="1"
            BorderBrush="{StaticResource BorderColor}">
        <Border.Effect>
            <DropShadowEffect Color="Black"
                              Direction="270"
                              ShadowDepth="0"
                              BlurRadius="20"
                              Opacity="0.8"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Barra de título y tabs -->
            <Border Grid.Row="0"
                    x:Name="TitleBar"
                    Background="{StaticResource BgSecondary}"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="45">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Logo -->
                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                Margin="16,0,12,0"
                                VerticalAlignment="Center">
                        <TextBlock Text="Z"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource AccentColor}"
                                   Margin="0,0,4,0"/>
                        <TextBlock Text="abrownie"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Tabs -->
                    <Border Grid.Column="1"
                            MouseLeftButtonDown="StopDrag">
                        <ScrollViewer
                            Style="{StaticResource OperaGXScrollViewer}"
                            HorizontalScrollBarVisibility="Auto"
                            VerticalScrollBarVisibility="Disabled">
                            <ItemsControl x:Name="TabsControl"
                                          ItemsSource="{Binding}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource TabButtonStyle}"
                                                Click="Tab_Click"
                                                Tag="{Binding}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Title}"
                                                           MaxWidth="150"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,12,0"/>
                                                <Button Grid.Column="1"
                                                        Content="✕"
                                                        Width="18"
                                                        Height="18"
                                                        FontSize="10"
                                                        Padding="0"
                                                        Click="CloseTab_Click"
                                                        Tag="{Binding}"
                                                        Foreground="{StaticResource TextSecondary}"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        Cursor="Hand">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}"
                                                                                CornerRadius="4">
                                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                                              VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver"
                                                                         Value="True">
                                                                    <Setter Property="Background"
                                                                            Value="#FF3A3A"/>
                                                                    <Setter Property="Foreground"
                                                                            Value="White"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <!-- New Tab Button -->
                    <Button Grid.Column="2"
                            Content="+"
                            Style="{StaticResource NavButtonStyle}"
                            Click="NewTab_Click"
                            FontSize="18"
                            FontWeight="Bold"
                            Margin="4,0"
                            MouseLeftButtonDown="StopDrag"/>

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="3"
                                Orientation="Horizontal"
                                MouseLeftButtonDown="StopDrag"
                                Margin="8,0,8,0">
                        <Button Content="—"
                                Width="40"
                                Height="40"
                                Click="Minimize_Click"
                                Style="{StaticResource NavButtonStyle}"/>
                        <Button Content="▢"
                                Width="40"
                                Height="40"
                                Click="Maximize_Click"
                                Style="{StaticResource NavButtonStyle}"
                                Margin="4,0,0,0"/>
                        <Button Content="✕"
                                Width="40"
                                Height="40"
                                Click="Close_Click"
                                Foreground="{StaticResource TextSecondary}"
                                Margin="4,0,0,0">
                            <Button.Style>
                                <Style TargetType="Button"
                                       BasedOn="{StaticResource NavButtonStyle}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver"
                                                 Value="True">
                                            <Setter Property="Background"
                                                    Value="#FF3A3A"/>
                                            <Setter Property="Foreground"
                                                    Value="White"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Barra de navegación -->
            <Border Grid.Row="1"
                    x:Name="NavigationBar"
                    Background="{StaticResource BgPrimary}"
                    Padding="12,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="◄"
                            x:Name="BackButton"
                            Style="{StaticResource NavButtonStyle}"
                            Click="BackButton_Click"
                            ToolTip="Atrás"
                            Margin="0,0,4,0"/>

                    <Button Grid.Column="1"
                            Content="►"
                            x:Name="ForwardButton"
                            Style="{StaticResource NavButtonStyle}"
                            Click="ForwardButton_Click"
                            ToolTip="Adelante"
                            Margin="0,0,4,0"/>

                    <Button Grid.Column="2"
                            Content="⟳"
                            Style="{StaticResource NavButtonStyle}"
                            Click="ReloadButton_Click"
                            ToolTip="Recargar"
                            Margin="0,0,12,0"/>

                    <!-- AddressBar -->
                    <Border Grid.Column="3"
                            Background="{StaticResource BgSecondary}"
                            BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Height="40">
                        <TextBox x:Name="AddressBar"
                                 VerticalContentAlignment="Center"
                                 Padding="16,0"
                                 FontSize="14"
                                 Foreground="{StaticResource TextPrimary}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 CaretBrush="{DynamicResource AccentColor}"
                                 KeyDown="AddressBar_KeyDown"
                                 GotFocus="AddressBar_GotFocus"
                                 Text="Escribe URL o busca..."/>
                    </Border>

                    <Button Grid.Column="4"
                            Content="Ir"
                            Style="{StaticResource ActionButtonStyle}"
                            Click="GoButton_Click"
                            Margin="8,0"/>

                    <Button Grid.Column="5"
                            x:Name="BookmarkButton"
                            Content="☆"
                            Style="{StaticResource NavButtonStyle}"
                            Click="BookmarkButton_Click"
                            FontSize="18"
                            ToolTip="Marcar esta página"
                            Margin="0,0,4,0"/>

                    <Button Grid.Column="6"
                            Content="🔖"
                            FontWeight="Bold"
                            Style="{StaticResource NavButtonStyle}"
                            Click="ManageBookmarks_Click"
                            ToolTip="Gestionar marcadores"
                            Margin="0,0,4,0"/>

                    <Button Grid.Column="7"
                            Content="⚙"
                            Style="{StaticResource NavButtonStyle}"
                            Click="SettingsButton_Click"
                            ToolTip="Configuración"/>
                </Grid>
            </Border>

            <!-- Barra de marcadores -->
            <Border Grid.Row="2"
                    x:Name="BookmarksBar"
                    Background="{StaticResource BgSecondary}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="0,1,0,0"
                    Padding="12,8"
                    Visibility="Collapsed">
                <ScrollViewer
                    Style="{StaticResource OperaGXScrollViewer}"
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="BookmarksBarControl">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Title}"
                                        Tag="{Binding Url}"
                                        Click="BookmarkBarItem_Click"
                                        Style="{StaticResource NavButtonStyle}"
                                        Width="Auto"
                                        Padding="12,6"
                                        Margin="0,0,4,0"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Main Content Area: Homepage OR WebView -->
            <Grid Grid.Row="3" Background="{StaticResource BgPrimary}">
                <!-- Homepage Container (shown when no page is loaded) -->
                <Grid x:Name="HomepageGrid"
                      Background="{StaticResource BgPrimary}">

                    <!-- Main Homepage Content -->
                    <ScrollViewer Style="{StaticResource OperaGXScrollViewer}"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,40,0,0">

                            <!-- Personal Greeting Section -->
                            <StackPanel HorizontalAlignment="Center"
                                        Margin="0,0,0,40">
                                <TextBlock Text="Welcome to"
                                           FontSize="16"
                                           Foreground="{StaticResource TextSecondary}"
                                           HorizontalAlignment="Center"/>

                                <TextBlock Text="Zabrownie"
                                           FontSize="48"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource AccentColor}"
                                           HorizontalAlignment="Center"
                                           Margin="0,8,0,16"/>

                                <!-- Personalized Message - You can customize this -->
                                <TextBlock x:Name="PersonalGreeting"
                                           Text="Your personal browsing experience ❤️"
                                           FontSize="18"
                                           Foreground="{StaticResource TextPrimary}"
                                           HorizontalAlignment="Center"
                                           Opacity="0.9"/>
                            </StackPanel>

                            <!-- Search Box (Center Stage) -->
                            <Border Background="{StaticResource BgSecondary}"
                                    CornerRadius="12"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1"
                                    Width="600"
                                    Height="55"
                                    HorizontalAlignment="Center"
                                    Margin="0,0,0,40">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="🔍"
                                               FontSize="18"
                                               VerticalAlignment="Center"
                                               Margin="16,0,12,0"
                                               Foreground="{StaticResource TextSecondary}"/>

                                    <TextBox x:Name="HomepageSearchBox"
                                             Grid.Column="1"
                                             Text="Search or enter address..."
                                             Foreground="{StaticResource TextPrimary}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             FontSize="14"
                                             VerticalContentAlignment="Center"
                                             KeyDown="HomepageSearchBox_KeyDown"
                                             GotFocus="HomepageSearchBox_GotFocus"
                                             LostFocus="HomepageSearchBox_LostFocus"/>

                                    <Button Grid.Column="2"
                                            Content="Go"
                                            Style="{StaticResource ActionButtonStyle}"
                                            Margin="8"
                                            Click="HomepageSearchButton_Click"/>
                                </Grid>
                            </Border>

                            <!-- Quick Links Section -->
                            <StackPanel HorizontalAlignment="Center"
                                        Width="600"
                                        Margin="0,0,0,40">
                                <TextBlock Text="Quick Links"
                                           FontSize="20"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimary}"
                                           Margin="0,0,0,16"/>

                                <WrapPanel Orientation="Horizontal"
                                           HorizontalAlignment="Center">
                                    <!-- Default Quick Links - You can customize these -->
                                    <Button Content="YouTube"
                                            Style="{StaticResource NavButtonStyle}"
                                            Click="QuickLink_Click"
                                            Tag="https://youtube.com"
                                            Margin="4"
                                            Padding="16,10"
                                            Width="120"/>

                                    <Button Content="Netflix"
                                            Style="{StaticResource NavButtonStyle}"
                                            Click="QuickLink_Click"
                                            Tag="https://netflix.com"
                                            Margin="4"
                                            Padding="16,10"
                                            Width="120"/>

                                    <Button Content="Spotify"
                                            Style="{StaticResource NavButtonStyle}"
                                            Click="QuickLink_Click"
                                            Tag="https://spotify.com"
                                            Margin="4"
                                            Padding="16,10"
                                            Width="120"/>

                                    <Button Content="Gmail"
                                            Style="{StaticResource NavButtonStyle}"
                                            Click="QuickLink_Click"
                                            Tag="https://gmail.com"
                                            Margin="4"
                                            Padding="16,10"
                                            Width="120"/>

                                    <Button Content="GitHub"
                                            Style="{StaticResource NavButtonStyle}"
                                            Click="QuickLink_Click"
                                            Tag="https://github.com"
                                            Margin="4"
                                            Padding="16,10"
                                            Width="120"/>

                                    <Button Content="Reddit"
                                            Style="{StaticResource NavButtonStyle}"
                                            Click="QuickLink_Click"
                                            Tag="https://reddit.com"
                                            Margin="4"
                                            Padding="16,10"
                                            Width="120"/>
                                </WrapPanel>

                                <!-- Add Custom Link Button -->
                                <Button Content="+ Add Custom Link"
                                        Style="{StaticResource NavButtonStyle}"
                                        Click="AddCustomLink_Click"
                                        Margin="0,16,0,0"
                                        HorizontalAlignment="Center"
                                        Padding="12,8"/>
                            </StackPanel>

                            <!-- Recently Visited (Optional) -->
                            <Border Background="{StaticResource BgSecondary}"
                                    CornerRadius="12"
                                    Padding="20"
                                    Width="600"
                                    HorizontalAlignment="Center">
                                <StackPanel>
                                    <TextBlock Text="Recently Visited"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimary}"
                                               Margin="0,0,0,12"/>

                                    <ItemsControl x:Name="RecentSitesControl">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding Title}"
                                                        Style="{StaticResource NavButtonStyle}"
                                                        Click="RecentSite_Click"
                                                        Tag="{Binding Url}"
                                                        Margin="0,4"
                                                        HorizontalContentAlignment="Left"
                                                        Padding="12,8"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <TextBlock x:Name="NoRecentSitesText"
                                               Text="No recent sites yet. Start browsing!"
                                               Foreground="{StaticResource TextSecondary}"
                                               FontStyle="Italic"
                                               HorizontalAlignment="Center"
                                               Margin="0,12,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Time/Date Widget -->
                            <Border Background="{StaticResource BgSecondary}"
                                    CornerRadius="12"
                                    Padding="20"
                                    Width="600"
                                    HorizontalAlignment="Center"
                                    Margin="0,20,0,0">
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center">
                                    <TextBlock x:Name="TimeText"
                                               Text=""
                                               FontSize="32"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource AccentColor}"
                                               VerticalAlignment="Center"/>

                                    <StackPanel Margin="20,0,0,0"
                                                VerticalAlignment="Center">
                                        <TextBlock x:Name="DateText"
                                                   Text=""
                                                   FontSize="14"
                                                   Foreground="{StaticResource TextPrimary}"/>

                                        <TextBlock x:Name="DayText"
                                                   Text=""
                                                   FontSize="12"
                                                   Foreground="{StaticResource TextSecondary}"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <!-- WebView Container (shown when browsing) -->
                <Grid x:Name="WebViewContainerGrid">
                    <Grid x:Name="WebViewContainer"/>

                    <!-- Floating Notification -->
                    <Border x:Name="StatusNotification"
                            Background="{StaticResource BgSecondary}"
                            BorderBrush="{DynamicResource AccentColor}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="12,8"
                            Margin="16"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Visibility="Collapsed">
                        <Border.Effect>
                            <DropShadowEffect Color="Black"
                                              Direction="270"
                                              ShadowDepth="0"
                                              BlurRadius="10"
                                              Opacity="0.5"/>
                        </Border.Effect>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="StatusText"
                                       Foreground="{StaticResource TextPrimary}"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
            
            <Rectangle
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Width="16"
                Height="16"
                Cursor="SizeNWSE"
                Fill="Transparent"
                MouseDown="ResizeGrip_MouseDown"/>
        </Grid>
    </Border>
</Window>